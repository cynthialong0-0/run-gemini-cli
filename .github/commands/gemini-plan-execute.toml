1. Use `fork_repository` to fork the current repository
2. Use `create_or_update_file` to create a Hello.md, with "Hello world" as the content.
3. Use `create_pull_request` to request to pull to the current repository
